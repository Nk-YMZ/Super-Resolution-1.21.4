# 构建

## 通用
* 确保已安装CMake或更高版本。
* 确保已安装Vulkan SDK。
* 确保已安装C++编译器。

## 对于Windows
1. 确保已安装Visual Studio 2019或更高版本。
2. 在根目录运行 `build_windows.ps1`。
3. 构建产物位于 `outpus/` 目录下。

## 对于Linux
1. 在根目录运行 `build_linux.sh`。
2. 构建产物位于 `outpus/` 目录下。
